{
  "name": "crf-ts-template",
  "version": "1.0.0",
  "description": "Cloud Run Functions TypeScript Template",
  "main": "dist/index.js",
  "config": {
    "project": "my-project-name",
    "functionsName": "my-function-name"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch --clear-screen=false node_modules/@google-cloud/functions-framework/build/src/main.js --target=entryPoint --source=src/index.ts",
    "deploy": "npm run build && gcloud functions deploy $npm_package_config_functionsName --project=$npm_package_config_project --gen2 --runtime=nodejs24 --region=asia-northeast1 --source=. --entry-point=entryPoint --trigger-http --allow-unauthenticated --set-build-env-vars=GOOGLE_NODE_RUN_SCRIPTS=",
    "lint": "biome check src",
    "fix": "biome check --write src",
    "prepare": "scripts/prepare.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.1",
    "@hono/node-server": "^1.19.7",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.10",
    "@types/node": "^25.0.3",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "lint-staged": {
    "src/**/*.{js,jsx,ts,tsx}": [
      "biome check --write"
    ]
  }
}
